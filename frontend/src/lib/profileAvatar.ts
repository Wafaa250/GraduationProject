import { initialsFromName } from '@/lib/doctorHubMappers'

/** Matches student profile hero — purple fill + white initial when no photo */
export const PROFILE_AVATAR_FALLBACK_CLASS = 'profile-avatar-fallback'

export { initialsFromName as profileInitialsFromName }
