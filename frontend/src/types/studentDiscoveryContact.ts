/** Contact details shown to companies after AI discovery — outreach happens off-platform. */
export type StudentDiscoveryContact = {
  email?: string | null;
  linkedin?: string | null;
  github?: string | null;
  portfolio?: string | null;
};
