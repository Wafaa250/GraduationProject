import { StudentPlaceholderScreen } from "@/components/student/StudentPlaceholderScreen";

export default function SettingsScreen() {
  return (
    <StudentPlaceholderScreen
      title="Settings"
      description="Account, security, and notification preferences will be available in an upcoming update."
    />
  );
}
